import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#32AD80",
    },
    secondary: {
      main: "#FCA56D",
    },
    error: {
      main: "#F0635A",
    },
    info: {
      main: "#81AC80",
    },
    success: {
      main: "#FFFFFF",
    },
  },
});

export default theme;
