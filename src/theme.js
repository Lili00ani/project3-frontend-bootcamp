import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#5A5A5A",
    },
    secondary: {
      main: "#0DCDAA",
    },
    error: {
      main: "#F0635A",
    },
    info: {
      main: "#81AC80",
    },
  },
});

export default theme;
